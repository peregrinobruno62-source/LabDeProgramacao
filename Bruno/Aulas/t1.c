#include <stdio.h>

int main () {

    float x, y;
    int qtdPontos;

    printf("Informe o par ordenado (x, y): ");
    scanf("%f %f", &x, &y);

    printf("Informe o número n de pontos: ");
    scanf("%d", &qtdPontos);

    
}